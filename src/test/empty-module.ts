// Stub for `server-only` (and similar guard packages) under Vitest.
// The real guard still applies in the Next.js build; tests just need the
// server-side modules to import without the client-boundary throw.
export {};
